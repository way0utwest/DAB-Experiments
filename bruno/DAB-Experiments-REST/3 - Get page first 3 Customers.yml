info:
  name: 3 - Get page first 3 Customers
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:5000/api/Customers?$first=3
  params:
    - name: $first
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
