info:
  name: 4 - Get one customer
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:5000/api/Customers?$filter=CustomerID eq 'ALFKI'
  params:
    - name: $filter
      value: CustomerID eq 'ALFKI'
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
