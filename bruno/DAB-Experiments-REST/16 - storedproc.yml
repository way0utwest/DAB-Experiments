info:
  name: 16 - storedproc
  type: http
  seq: 16

http:
  method: POST
  url: http://localhost:5000/api/CustomerOrderHistory?CustomerID=ANATR
  params:
    - name: CustomerID
      value: ANATR
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
