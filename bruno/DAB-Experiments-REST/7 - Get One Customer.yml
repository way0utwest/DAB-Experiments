info:
  name: 7 - Get One Customer
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5000/api/Customers?$filter=CustomerID eq 'ALFKI'
  params:
    - name: $filter
      value: CustomerID eq 'ALFKI'
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
