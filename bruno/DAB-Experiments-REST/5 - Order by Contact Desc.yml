info:
  name: 5 - Order by Contact Desc
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5000/api/Customers?$orderby=ContactName desc
  params:
    - name: $orderby
      value: ContactName desc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
