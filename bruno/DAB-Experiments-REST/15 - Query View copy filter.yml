info:
  name: 15 - Query View copy filter
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:5000/api/SalesSummary?$filter=OrderID eq 10248
  params:
    - name: $filter
      value: OrderID eq 10248
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
