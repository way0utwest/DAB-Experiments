info:
  name: Get Wildcard Page of 3
  type: graphql
  seq: 7

graphql:
  method: POST
  url: http://localhost:5000/graphql
  body:
    query: |-
      query Customers {
          customers( first:3
              filter: { CustomerID: { startsWith: "F" } }
          ) {
              endCursor
              hasNextPage
              items {
                  CustomerID
                  CompanyName
              }
             hasNextPage
          endCursor
          }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
