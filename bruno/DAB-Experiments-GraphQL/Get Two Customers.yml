info:
  name: Get Two Customers
  type: graphql
  seq: 3

graphql:
  method: POST
  url: http://localhost:5000/graphql
  body:
    query: |2-
        query Customers {
          customers(
              filter: { or: [{CustomerID: { eq: "VINET" } }
              { CustomerID: {eq: "BONAP"}}]}
          ) {
              endCursor
              hasNextPage
              items {
                  CustomerID
                  CompanyName
              }
          }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
