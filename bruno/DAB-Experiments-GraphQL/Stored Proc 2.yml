info:
  name: Stored Proc 2
  type: graphql
  seq: 15

graphql:
  method: POST
  url: http://localhost:5000/graphql
  body:
    query: |-
      query {
        executeCustomerOrdersDetail(OrderID: 10248) {
          ProductName
          Quantity
          ExtendedPrice
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
