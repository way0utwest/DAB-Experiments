info:
  name: Stored Proc
  type: graphql
  seq: 13

graphql:
  method: POST
  url: http://localhost:5000/graphql
  body:
    query: |-
      query {
        executeCustomerOrderHistory(CustomerID: "ANATR") {
          ProductName
          Total
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
