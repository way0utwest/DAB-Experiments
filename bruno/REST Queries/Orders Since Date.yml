info:
  name: Orders Since Date
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:5000/api/Orders?$filter=OrderDate ge 1998-01-01T00:00:00Z
  params:
    - name: $filter
      value: OrderDate ge 1998-01-01T00:00:00Z
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
