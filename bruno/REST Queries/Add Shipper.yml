info:
  name: Add Shipper
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:5000/api/Shippers
  auth: inherit
  body:
    mode: json
    json: |-
      {
        "CompanyName": "Redgate Software"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
