info:
  name: Orders by Ship Country
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5000/api/Orders?$filter=ShipCountry eq 'France'
  params:
    - name: $filter
      value: ShipCountry eq 'France'
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
