info:
  name: Order by ID
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:5000/api/Orders?$filter=OrderID eq 10253
  params:
    - name: $filter
      value: OrderID eq 10253
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
