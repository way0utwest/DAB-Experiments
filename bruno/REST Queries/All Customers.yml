info:
  name: All Customers
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5000/api/Customers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
